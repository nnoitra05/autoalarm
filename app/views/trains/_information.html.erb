<div class="train_information">
  <div class="train_title">
      <h2>運行情報</h2>
  </div>

  
  <div class="train_contents">
    <% @information_list.each do |info| %>
      <li><%= info %></li><br>
    <% end %>
  

    <%= "現在、首都圏の遅延情報はありません。" if @information_list.empty? %>
  </div>
  
</div>